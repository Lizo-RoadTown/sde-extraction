# apps/

**Empty today.** Placeholder for future website/frontend code.

When the research surfaces a product shape AND Liz decides to build it:

- This is where the frontend (website / UI / dashboard) goes
- Stack TBD — likely Next.js or Vite + React per Liz's defaults
- At that point, a new instance (`sde-extraction-app`) gets attached in `.project-intelligence/`
- Adapter shifts from research-project → product-shaped (whichever fits)

Until then, leave this dir empty (just the README + .gitkeep).
