# services/

**Empty today.** Placeholder for future backend services.

When the research surfaces a need for server-side capability:

- This is where backend services go (API, data ingestion, processing pipelines, etc.)
- Stack TBD — likely Python (FastAPI) for data-adjacent work; Node if primarily a JSON API
- Deploys to Render per Liz's defaults
- Per-service subdirectories (e.g., `services/api/`, `services/ingest/`, `services/etl/`)

Until then, leave this dir empty (just the README + .gitkeep).
