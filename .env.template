# Copy to .env and fill in. .env is gitignored.

# Loom Project ID — for the developer/research instance
LOOM_PROJECT_ID=sde-extraction-dev

# OTel telemetry — copy from the-loom's .env or inherit from User-scope env vars
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_EXPORTER_OTLP_HEADERS=

# Future: when the deployed app/UI lands, a second instance will need its own env:
#   LOOM_PROJECT_ID=sde-extraction-app
# Set on the deployment platform (Render / Vercel / etc.), NOT in this local .env.
